[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
macroquad = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde-wasm-bindgen = "0.6" # Nützlich für späteren WASM <-> JS Bridge, falls nötig